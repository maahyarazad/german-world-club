/**
 * Member marketplace — the shapes the server and every client share
 * (BUSINESS_DESCRIPTION.md §7, feature 008).
 *
 * The category field definitions live here rather than in either client because
 * the console's compose form and the server's validation must be the same
 * definition (Principle I). A client may shape its form from this; it is never
 * the enforcement point.
 */
